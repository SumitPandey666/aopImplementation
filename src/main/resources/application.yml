spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aop_crud_db
    username: ${username}
    password: ${password}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

debug: true
#
#logging:
#  level:
#    org.springframework.cloud.vault: DEBUG